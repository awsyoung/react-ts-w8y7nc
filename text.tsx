import React, { Component } from 'react';

const Text = (props) => {
  return (
    <div>
        I created a React functional component and passed its value with a prop!
    </div>
  );
};
export default Text;
